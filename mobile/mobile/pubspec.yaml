name: agrofamily
description: AGROFAMILY - connecting the agriculture chain in Cameroon
publish_to: "none"
version: 0.5.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  dio: ^5.5.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^6.0.5
  flutter_vlc_player: ^7.4.1
  geolocator: ">=13.0.1 <99.0.0"
  camera: ^0.11.0
  video_compress: ^3.1.3
  image_picker: ^1.1.2
  intl: ^0.19.0
  flutter_secure_storage: ^9.2.2
  qr_code_scanner_plus: ">=2.0.0 <99.0.0"
  pin_code_fields: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
